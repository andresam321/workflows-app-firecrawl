{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "id": "url",
      "type": "string",
      "label": "Website URL",
      "description":"Enter the website you want to map (e.g., 'https://example.com')",
      "validation": {
        "required": true
      }
    },
    {
  "id": "search_term",
  "type": "string",
  "label": "Filter by Keyword (optional)",
  "description": "Only return links that include this word or phrase (e.g., 'blog', 'pricing')"
}
  ],
  "ui_options": {
    "ui_order": ["url","search_term"]
  }
}
